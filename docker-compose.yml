services:
  asgen-web-ui:
    image: asgen-web-ui
    build:
      context: .
      dockerfile: Dockerfile
    container_name: asgen-web-ui
    ports:
      - '${PORT:-3000}:${PORT:-3000}'
    env_file:
      - .env
